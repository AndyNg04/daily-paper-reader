---
title: Linguistic Monoculture in LLM-Assisted Language Use
title_zh: 大型语言模型辅助语言使用中的语言单一文化
authors: "Suhas Thejaswi, Juhi Kulshreshta, Lutz Oettershagen"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27134v1"
tags: ["query:human-ai"]
score: 9.0
evidence: LLM辅助写作; 人机交互机制; 反馈与个性化
tldr: LLM辅助写作虽提升清晰度，但广泛使用共享模型可能引发语言单文化，即群体语言多样性下降。我们建立数学框架，将作者与LLM表示为语言特征分布，并分析固定共享模型、递归更新及个性化三种交互机制。结果显示共享模型驱动趋同，递归反馈仅移动规范而不改变离散度，个性化则保留多样性。个体理性趋同产生负外部性，导致社会次优的“单文化代价”。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27134v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1265, \"height\": 459, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27134v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1589, \"height\": 533, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27134v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 823, \"height\": 529, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27134v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1600, \"height\": 909, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27134v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1604, \"height\": 548, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27134v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1621, \"height\": 554, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27134v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1674, \"height\": 567, \"label\": \"Figure\"}]"
motivation: 大规模语言模型普及可能削弱语言多样性，但缺乏理论分析，需要量化机制与影响。
method: 构建作者与LLM共演化的数学框架，分析三种交互机制下的均衡与收敛速率，并内生化趋同策略。
result: 共享模型致趋同，递归反馈不降离散度，个性化保留多样性；个体趋同产生负外部性且代价可无限增长。
conclusion: 语言单文化是社会困境，需通过个性化或制度设计平衡效率与多样性。
---

## 摘要
写作和沟通日益由大型语言模型（LLM）中介，这些模型被用于起草、修订和润色文本。尽管这种辅助可以提高清晰度并帮助作者满足机构期望，但对共享模型的广泛依赖可能会减少语言形式的群体水平变异，我们将这种现象称为语言单一文化。我们开发了一个数学框架，其中作者和LLM被表示为语言特征上的分布，并通过反复互动共同进化。我们分析了三种互动机制：具有固定语言分布的共享模型、根据作者输出递归更新的共享模型，以及通过作者特定和群体水平反馈更新的个性化模型。我们刻画了由此产生的均衡和收敛速率，表明：共享模型可以推动作者趋向共同规范；在共同从众性下，递归反馈重新定位共享规范而不改变成对差异；个性化可以保留一系列具有非零语言多样性的不同作者-模型均衡。然后，我们将从众性内生化，视为一种战略选择，在清晰度、易读性和感知流畅性带来的私人收益与独特风格之间进行权衡。在这个效用模型中，个体理性的作者可能比社会最优状态下更倾向于从众，因为他们没有内化其独特性为他人的价值，从而产生负外部性和单一文化的代价，该代价对于每个固定实例是有限的，但当独特性主导真实性时可能无界增长。合成模拟说明了固定的共享辅助、递归反馈和个性化如何产生不同的长期多样性结果。

## Abstract
Writing and communication are increasingly mediated by large language models (LLMs) that are being used to draft, revise and polish text. Although such assistance can improve clarity and help authors meet institutional expectations, widespread reliance on shared models may reduce population-level variation in linguistic form, a phenomenon we refer to as linguistic monoculture. We develop a mathematical framework in which authors and LLMs are represented as distributions over linguistic features and coevolve through repeated interaction. We analyze three interaction mechanisms: a shared model with a fixed linguistic distribution, a shared model recursively updated from author outputs, and personalized models updated through author-specific and population-level feedback. We characterize the resulting equilibria and convergence rates, showing that, shared models can drive authors toward a common norm, recursive feedback relocates the shared norm without altering pairwise spread under common conformity, and personalization can preserve a family of distinct author-model equilibria with nonzero linguistic diversity. We then endogenize conformity as a strategic choice trading off private benefits from clarity, legibility, and perceived fluency against distinctive style. Within this utility model, individually rational authors may conform more than is socially optimal because they do not internalize the value their distinctiveness provides to others, creating a negative externality and a price of monoculture that is finite for each fixed instance but can grow without bound when distinctiveness dominates authenticity. Synthetic simulations illustrate how fixed shared assistance, recursive feedback, and personalization produce different long-run diversity outcomes.

---

## 论文详细总结（自动生成）

## 一、论文的核心问题与整体含义

- **研究动机**：大规模语言模型（LLM）被广泛用于起草、修订和润色文本，能够提升写作清晰度、减少错误并帮助作者满足机构期望。然而，当大量作者依赖共享模型时，他们的语言表达可能趋向同质化，导致群体层面的语言多样性下降——作者将这一现象称为**语言单一文化（linguistic monoculture）**。
- **核心问题**：在何种形式的作者–模型反复交互下，LLM辅助会使群体趋向共享语言规范？何时异质偏好或个性化机制能够保留语言多样性？
- **整体含义**：该论文从理论上刻画了LLM辅助写作与语言多样性之间的张力。语言多样性不仅是美学问题，还能帮助作者建立个人风格、使观点区分度更清晰，是学术和公共表达中的重要资源。研究最终将问题定位为**社会困境**：个体理性地追求清晰度和规范性，却可能损害群体共享的语言多样性，产生一种负外部性。
- **研究层面**：论文关注的是语言特征分布的多样性（词汇选择、句法结构、话语标记等），而非语义内容或知识层面的趋同。

## 二、方法论：核心思想、技术细节与公式

### 1. 核心思想：分布化表示与耦合演化

- 将每位作者和LLM都表示为定义在有限语言特征集合 F（m 个特征）上的概率分布：
  - 作者 i 在时间 t 的写作风格分布为 pᵢᵗ ∈ Δᵐ⁻¹；
  - LLM诱导的语言分布为 qᵗ（共享时）或 qᵢᵗ（个性化时）。
- 作者更新规则：
  - pᵢᵗ⁺¹ = (1 − αᵢ) pᵢᵗ + αᵢ Aᵢ(qᵢᵗ, zᵢᵗ)
  - 其中 αᵢ ∈ [0,1] 是作者 i 的适应速率，Aᵢ 是将模型输出和作者上下文信息映射为新分布的算子。
- 模型递归更新规则（当模型会吸收作者输出时）：
  - qᵗ⁺¹ = β qᵗ + (1 − β) B(pᵗ⁺¹₁, ..., pᵗ⁺¹ₙ)
  - 其中 β 是模型保留参数，B 是作者输出的聚合算子（通常取加权平均）。
- **核心度量**：群体语言多样性采用**平均成对Jensen–Shannon散度（JS散度）**：Dt = (1/n(n−1)) Σᵢ≠ⱼ JS(pᵢᵗ, pⱼᵗ)；辅助度量包括作者–模型对齐度 Mt 和个性化模型间的多样性 Qt。

### 2. 三种交互机制（IM）

| 机制 | 描述 | 关键公式与性质 |
|---|---|---|
| **IM 1：固定共享模型** | 所有作者共享同一个固定语言分布的模型 | pᵢᵗ⁺¹ = (1−αᵢ)pᵢᵗ + αᵢ Aᵢ(q⁰, zᵢᵗ)；同质适应下多样性以指数速率衰减（t ≥ log(2/ε)/α_min）；异质适应下每位作者收敛到各自的目标分布，多样性由目标间差异性决定 |
| **IM 2：递归更新的共享模型** | 所有作者共享模型，但模型会吸收作者输出进行更新 | 结合作者更新与 qᵗ⁺¹ = βqᵗ + (1−β)Σwᵢpᵢᵗ⁺¹；收敛到内生均衡 pᵢ* = (1−λᵢ)rᵢ + λᵢq*，q* = Σwᵢ(1−λᵢ)rᵢ / (1−Σwᵢλᵢ)；递归反馈将共享规范从外生 q⁰ 重新定位到内生 q*，但不改变成对几何差异 |
| **IM 3：递归更新的个性化模型** | 每位作者使用自己专属模型，反馈混合个人与群体信息 | qᵢᵗ⁺¹ = βqᵢᵗ + γpᵢᵗ⁺¹ + δΣⱼwⱼpⱼᵗ⁺¹；均衡时 pᵢ* = ηᵢrᵢ + (1−ηᵢ)P*，qᵢ* = ρηᵢrᵢ + (1−ρηᵢ)P*；当 ρ > 0 时，保留作者特异的均衡分量，维持正的语言多样性 |

### 3. 关键理论结果

- **收敛速率**：
  - IM 1 固定模型下以 α_min 控制指数收敛；
  - IM 2/3 递归设置下最坏情况收敛速率变慢（受 (1−β)·αᵢ·(1−λᵢ) 控制），但实际可能更快。
- **对比命题（Proposition 3.6）**：
  - 在共同从众水平 λ 下，IM 1 和 IM 2 的极限成对差异相同（都是 (1−λ)(rᵢ−rⱼ)），二次多样性相同；
  - IM 3 的二次多样性是 IM 1 的 1/(1−ρλ)² 倍，即个性化从几何上**扩大**了作者间的差异。

### 4. 战略性从众与单一文化代价（Section 4）

- 将从众参数 λᵢ 内生化：作者在**合法性收益**（接近模型规范 q⁰）、**真实性成本**（偏离自身偏好 rᵢ）与**区分度收益**（与其他作者不同）之间权衡。
- 效用函数（正交签名假设下）：
  - Uᵢ(λ) = (dᵢ²/2)[(θᵢ−bᵢ)σᵢ² − cᵢ(1−σᵢ)²] + (θᵢ/2(n−1))Σ_{j≠i} σⱼ²dⱼ²
  - σᵢ = 1 − λᵢ（保留的独特性），dᵢ² = ‖rᵢ − q⁰‖²（偏离规范的距离），bᵢ/θᵢ/cᵢ 分别为清晰度收益、区分度权重和真实性成本。
- **主要结论**：
  - 每个作者都存在严格占优策略，唯一的纳什均衡为 λᴺᴱᵢ = (bᵢ−θᵢ)₊ / ((bᵢ−θᵢ)₊ + cᵢ)；
  - 社会最优为 λˢᴼᵢ = (bᵢ−θᵢ−θ̄₋ᵢ)₊ / ((bᵢ−θᵢ−θ̄₋ᵢ)₊ + cᵢ)；
  - **λᴺᴱ ≥ λˢᴼ**，即个体理性导致过度从众，因为作者没有内化其独特性为他人的价值（负外部性）。
- **单一文化代价（PoM）**：对称设定下有闭合解，在三个区间内分别为 PoM = 1（无效率）、PoM = ((b+c−θ)/c)²（纯浪费性从众）以及 PoM = ((b+c−θ)/(b+c−2θ))²（内部过度从众，可随 θ/c → ∞ 无界增长）。

## 三、实验设计

- **模拟场景**：合成模拟，没有真实数据集。
  - n = 100 位作者，m = 10 个抽象语言特征，T = 200 步，100 次独立配对运行。
  - 初始分布 p⁰ᵢ、q⁰ 和偏好分布 rᵢ 独立采样自 Dirichlet(1,...,1)。
  - αᵢ ~ Uniform[0.05, 0.50)，λᵢ ~ Uniform[0,1)，作者权重 wᵢ = 1/n。
  - 基线参数：β = 0.25，IM 3 中 ρ = 2/3（γ = 0.5，δ = 0.25）。
- **对比方法**：核心对比三种交互机制 IM 1 vs. IM 2 vs. IM 3在同一初始化下的长期多样性差异；额外对比不同个人化水平 ρ（0 vs. 2/3 vs. 1）对多样性的影响。
- **消融与稳健性实验**（附录 F）：
  1. **Dirichlet 浓度**（a = 0.1, 1, 5）对多样性轨迹的影响；
  2. **从众异质性**（λᵢ 的宽度 w = 0, 0.25, 0.5）对 IM 1–IM 2 差异来源的分解（位置效应 vs. 几何效应）；
  3. **群体规模**（n = 100 vs. n = 500）的稳健性；
  4. **辅助度量**（作者–模型散度 Mt、个性化模型多样性 Qt、平移不变二次多样性 D̂t）；
  5. **混合机制 IM 4**（1/3 群体走 IM 1，1/3 走 IM 2，1/3 走 IM 3）。

## 四、资源与算力

- 论文**未明确报告** GPU 型号、数量或训练时长。
- 仅提及所有实验在**普通笔记本电脑**上执行，用 Python 标准数值库实现。附录声明 LLM 仅用于辅助编码和文本润色。这表明实验的计算开销较小，属于合成模拟的轻量级计算，不涉及大规模模型训练。

## 五、实验数量与充分性评估

- **实验数量**：主实验（3种机制对比 + 1组个人化敏感性）+ 6 组附录实验（浓度、异质宽度、群体规模、辅助度量、IM 4、轨迹检查），共约 8 组相对完整的实验场景，每组基于 100 次独立配对运行。
- **充分性**：
  - 论文的**内部一致性较好**：配对运行确保各机制从相同初始化出发，实验能有效支持理论命题（如 Proposition 3.5 预测的 ρ 与多样性关系、Proposition 3.6 预测的二次几何差异）。
  - **局限性明显**：所有实验均为**合成数据**，没有真实语料或真实用户写作数据，缺乏对模型参数（α、λ、β、ρ）的经验校准。作者明确承认这一点，并指出模拟旨在说明定性行为而非提供定量预测。因此，实验在“理论验证”层面充分，在“现实适用性”层面不足。

## 六、主要结论与发现

1. **共享模型驱动趋同**：固定共享模型在共同适应目标下使语言多样性以指数速率衰减，衰减速度受最慢适应作者的控制。
2. **递归反馈改变目标但不改变分散度**：在共同从众水平下，递归更新的共享模型只将均衡规范从外生 q⁰ 移动到内生 q*，作者间的成对几何差异与固定模型相同。
3. **个性化保留多样性**：个性化模型（IM 3）保留了作者特有的均衡分量，当个人化程度 ρ 增大时，作者间多样性单调上升；当 ρ = 1 时，多样性可恢复到接近初始水平。
4. **个体理性导致过度从众**：由于独特性是共享资源，每个作者的从众行为会降低其他作者的效用（负外部性），纳什均衡下的从众水平系统性高于社会最优水平。
5. **单一文化代价可无限增长**：当独特性价值（θ）相对于真实性成本（c）无界增大时，社会最优与均衡之间的多样性差距可以无界增大。
6. **并非所有趋同都是坏的**：当独特性没有价值（θ = 0）时，从众就是有益的标准化，PoM = 1 且无福利损失。

## 七、优点

- **理论框架系统完整**：从固定共享 → 递归共享 → 递归个性化，逐步引入耦合度，清晰隔离共享性、递归性和个性化三个维度的影响。
- **严谨的数学刻画**：对三种机制都给出了均衡表达式、收敛速率上界和比较静态分析，理论结论有完备证明。
- **战略性视角的新颖性**：将从众性内生化，用博弈论工具刻画了“个体理性 vs. 群体效率”的张力，将算法单一文化领域的价格无政府边界思想扩展到语言领域。
- **对实验结果的诚实解读**：区分了 JS 散度的位置依赖与真正的几何差异，使用平移不变二次诊断量对结论进行了交叉验证——这一方法学设计很细致。
- **广泛的外延讨论**：附录讨论了读者福利（Remark A.4）、递归更新下的战略性从众（Remark A.5）、相关签名（非正交）情形下的博弈论刻画，以及在单一群体中混合多种机制的 IM 4。

## 八、不足与局限

- **框架高度简化**：作者明确承认这是一个“简化形式”模型，忽略了非 LLM 语言暴露（合作者、审稿人、阅读材料、社交网络）的影响，不能视为现实世界中语言变化的完整模型。
- **实验全为合成数据**：没有使用真实语料或真实用户行为数据对模型参数进行校准，缺乏实证验证。作者指出这是未来工作的方向，但当前论文无法回答现实中语言单一文化发生的具体速率和程度。
- **战略模型的假设限制**：Section 4 的闭合解依赖于三个强假设：固定从众水平、二次效用函数、作者签名正交（要求 n ≤ m−1）。附录 D 虽然放松了正交性，但仅覆盖非正对齐签名的情形，正相关签名下外部性符号可能反转，结论不具普适性。
- **读者福利被忽略**：最优福利计算只计数作者的效用，即使如此，结论已经显示过度从众；如果考虑更广泛的读者/社会价值，效率损失只会更大——这意味着实际的社会代价被低估，但论文未提供量化方法。
- **边界情况未深入**：对 λᵢ = 1（完全从众）的奇异边界、模型崩溃动力学的关系仅在 remarks 中展开，未给出完整的博弈论均衡刻画。
- **人口规模与特征维度耦合**：正交签名的假设限制了可建模的作者数量，尽管论文指出在特征集足够丰富时并非限制，但理论推广仍需更多处理。

（完）
