---
title: Preference-Driven Online Adaptation for Personalized Interaction Initiation in Proactive AI Assistants
title_zh: 主动式AI助手中用于个性化交互启动的偏好驱动在线自适应
authors: "Yufeng Wang, Wei Zhang, Zhiquan Wen, Jinwu Hu, Linhui Xiao, Tianlu Pan, Qingfang Zheng, Mingkui Tan"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04416v1"
tags: ["query:human-ai"]
score: 9.0
evidence: 偏好驱动的主动AI助手交互时机在线适配
tldr: 现有AI助手多为被动响应，主动助手需自主判断交互时机，但适当时机因人而异且在线反馈稀疏。本文提出EOPA，将用户交互时机偏好锚定为时间偏好锚点与活动原型两类可测证据，经用户先验平滑与不确定性缩放后自适应融合决策，并利用LLM生成个性化回复，在线更新无需重训。实验显著提升交互时机F1与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.04416v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 829, \"height\": 744, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04416v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1747, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04416v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 781, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04416v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 781, \"height\": 296, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.04416v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1829, \"height\": 601, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04416v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 878, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04416v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 880, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04416v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 879, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04416v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 875, \"height\": 242, \"label\": \"Table\"}]"
motivation: 主动助手需要自主决定交互时机，但该时机高度个性化且难以预先建模，在线反馈虽能提供信号却因合适时刻稀疏而难以直接利用。
method: EOPA通过时间锚点与活动原型承载证据，结合用户先验平滑和不确定性缩放后自适应融合，决策交互时用LLM以历史示范生成响应，并从反馈在线更新。
result: 在ProPerSim基准上交互时机F1提升19.80点，静默与交互延迟大幅降低，日均适应时间从11.41秒降至0.39秒。
conclusion: EOPA以轻量在线适应高效实现个性化交互时机决策，无需LLM推理或重训练，为主动助手提供了实用且可扩展的范式。
---

## 摘要
AI助手通常是反应式的，依赖用户发起交互。主动式助手超越这一范式，根据用户的活动上下文自主发起交互。然而，适当的交互时机因人而异，很难预先确定，而在线反馈为个性化提供了有价值的信号。因此，直接的反馈驱动自适应很有吸引力，但由于值得交互的时刻稀疏地分布在细粒度的用户状态中，仍然具有挑战性。为了解决这些问题，我们提出了证据驱动的在线偏好自适应（EOPA），它通过两种证据载体将用户的交互时机偏好建立在可测量的上下文证据上：时间偏好锚点和带有证据的活动原型。在每个轮询步骤中，EOPA通过用户先验平滑的证据估计和不确定性引导的证据缩放从载体中提取时间和活动证据，并自适应地融合证据以做出交互或静默的决策。当选择交互时，大语言模型（LLM）使用高质量的历史响应作为示例，生成更符合用户偏好的上下文感知响应。EOPA从收到的在线反馈中更新其证据载体和决策参数，无需基于LLM的推理或重新训练。在基于ProPerSim的基准上的大量实验表明，与实验中最强的基线相比，EOPA将交互时机的F1分数提高了19.80个百分点，显著降低了静默和交互步骤的推理延迟，并将平均每日适应时间从11.41秒降低到0.39秒。

## Abstract
AI assistants are typically reactive, relying on users to initiate interactions. Proactive assistants go beyond this paradigm by autonomously initiating interactions based on users' activity contexts. However, appropriate interaction timing is user-specific and difficult to determine in advance, while online feedback offers valuable signals for personalization. Direct feedback-driven adaptation is therefore appealing, but remains challenging due to sparse interaction-worthy moments scattered across fine-grained user states. To address the issues, we propose Evidence-driven Online Preference Adaptation (EOPA), which grounds a user's interaction-timing preferences in measurable contextual evidence through two evidence carriers: temporal preference anchors and evidence-bearing activity prototypes. At each polling step, EOPA derives temporal and activity evidence from the carriers through user-prior-smoothed evidence estimation and uncertainty-guided evidence scaling, and adaptively fuses the evidence for interaction-or-silence decisions. When interaction is selected, an LLM uses high-quality historical responses as demonstrations to generate a context-aware response that better reflects user preferences. EOPA updates its evidence carriers and decision parameters from received online feedback without LLM-based reasoning or retraining. Extensive experiments on a ProPerSim-based benchmark show that EOPA improves the interaction-timing F1 score by 19.80 points over the strongest baseline in our experiments, substantially reduces inference latency for both silence and interaction steps, and lowers the average daily adaptation time from 11.41 to 0.39 seconds.

---

## 论文详细总结（自动生成）

# 《主动式AI助手中用于个性化交互启动的偏好驱动在线自适应》论文总结

## 1. 核心问题与研究动机

- 传统 AI 助手是**反应式**的，只能等待用户显式请求；而主动式 AI 助手需要根据用户当前活动上下文，**自主判断合适的交互时机**并主动发起帮助。
- 核心难点在于：交互时机**高度个性化**，同一情境对某个用户可能值得打扰，对另一用户则可能毫无必要；这种偏好难以预先规定，只能通过在线反馈逐步揭示。
- 然而，**值得交互的时刻非常稀疏**（在本文基准中仅占全部轮询步骤的 1.129%），且散落在细粒度的用户状态（时间 + 活动）中；相似状态可能对应完全不同的交互需求，使得直接学习或文本化经验复用都很困难。
- 现有方法主要有两条路线：
  - **参数学习方法**：微调 LLM 或模型预测交互动作，但稀疏反馈不足以学习细粒度时机偏好，且重训练成本高；
  - **LLM 自改进方法**：将轨迹和反馈总结为自然语言反思/规则，但语言经验的“适用边界”通常模糊，容易过度泛化或过度窄化。
- 本文的核心观点是：**将用户交互时机偏好“落地”为可测量的上下文证据**，通过统计不同时间、活动语义条件下的交互/静默反馈，保留相似状态之间的细粒度差异。

## 2. 方法论：EOPA

### 2.1 总体框架

EOPA（Evidence-driven Online Preference Adaptation，证据驱动的在线偏好自适应）由两条证据通道组成：

- **时间偏好锚点（Temporal Preference Anchors）**：刻画时间区间 × 活动进度条件下的交互/静默统计；
- **带证据的活动原型（Evidence-Bearing Activity Prototypes）**：刻画活动语义相似条件下的交互/静默统计，并随时间在线扩展。

### 2.2 技术关键点

- **时间偏好锚点**
  - 将每天分为 B 个固定长度时间区间，并记录当前活动段内的连续轮询进度 r；
  - 每个锚点保存 `(交互合适计数 N_INT, 静默偏好计数 N_SIL)`；
  - 匹配当前状态对应锚点后，利用**用户先验平滑**估计交互合适概率：

    `p_temp = (N_INT + κ·p_u) / (N_INT + N_SIL + κ)`

    其中 `p_u` 是 Jeffreys 平滑的全局用户交互先验率；κ 控制先验强度。

- **不确定性引导的证据缩放**
  - 使用 Beta 后验方差衡量锚点局部支持的可靠性：

    `σ² = p(1-p) / (N_INT + N_SIL + κ + 1)`

  - 最终证据分数为 `e = p / sqrt(σ² + ε)`；支持数据越多、时机越合适，证据值越高。

- **带证据的活动原型**
  - 用冻结文本编码器（all-mpnet-base-v2）将活动描述映射为向量；
  - 检索最相似的 K 个原型，按相关度加权聚合其交互/静默统计；
  - 相似度加权包含“覆盖抑制项”，避免当前活动与原型语义完全不相关时的证据过度迁移；
  - 每次反馈后更新最相似原型或创建新原型（相似度大于阈值 ρ 时更新，否则新建）。

- **证据融合与自适应决策**
  - 总分数 `s_t = e_temp,t + λ_t · e_act,t`，与阈值 `δ_t` 比较，决定交互或静默；
  - `λ_t` 和 `δ_t` 会基于近期反馈历史，通过重放候选参数对并优化 F1 进行在线校准；
  - 更新过程无需 LLM 推理或重训练，只有轻量统计更新与参数重校准。

- **响应生成与时机决策解耦**
  - 只有判定为“交互”时才调用 LLM；
  - 使用高质量历史响应作为 in-context 示例，以简洁、礼貌、上下文相关的方式生成回复；
  - 通过解耦，LLM 不参与时机判断，从而大幅节省推理开销。

## 3. 实验设计

### 3.1 基准与数据

- 基于 **ProPerSim** 构建：32 个多样化用户人设，使用 Generative Agents 模拟每日活动；
- 每个用户模拟 14 天，共 **448 个用户日、160,632 个轮询步骤**，轮询间隔 2.5 分钟；
- 交互时机标注由 Qwen3.5-397B-A17B 根据 ProPerSim 提供的时机/频率偏好生成，共 1,814 个交互合适时刻（仅 1.129%）；
- 配有额外人工评估，验证模拟活动与标注质量。

### 3.2 对比方法

- **参数学习**：ProPerAssistant；
- **LLM 自改进**：EvoTest、SCOPE、Reflexion；
- 不同基线分别使用 Qwen3-4B-Instruct-2507、DeepSeek-V4-Flash、GLM-5 作为自适应 LLM；
- 所有方法使用相同的轮询间隔、反馈协议、评估 prompt，且使用同一个 Qwen3-4B 模型生成响应。

### 3.3 评价指标

- 时序：F1、F1@1、F1@2、Recall、Precision、Accuracy；
- 频率匹配率；
- 个性偏好、沟通与安全性（由 LLM judge 评估）；
- 另设消融实验、鲁棒性（部分反馈）、在线动态、证据可判别性、相似状态判别准确率等分析。

## 4. 资源与算力

- 论文**没有明确给出 GPU 型号、数量或训练时长**；
- 需要指出：EOPA 本身**无需训练或 LLM 驱动的自我反思**，适应过程是轻量统计更新；
- 推理阶段使用的模型包括：Qwen3-4B-Instruct-2507（响应生成）、frozen all-mpnet-base-v2（活动编码）、Qwen3.5-397B-A17B（标注与评估）；
- 基线方法（Reflexion 等）在每一步都需要调用 LLM，因此延迟和计算成本显著更高。

## 5. 实验数量与充分性评估

- **实验数量较充足**，包括：
  1. 主对比实验（含 3 种自适应 LLM 的 4 个基线 vs. EOPA）；
  2. 增量消融实验（5 个组件逐步加入）；
  3. 在线适应动态分析（14 天逐日 F1）；
  4. 推理延迟与每日适应时间对比；
  5. 部分反馈保留率鲁棒性（100%/75%/50%/25%）；
  6. 证据可判别性分析（早期/中期/晚期）；
  7. 相似状态决策准确率对比。
- **公平性**：各方法共享任务设置、反馈协议、响应 LLM 与评测 prompt，控制变量较为规范；多个 LLM 作为基线自适应模型也增强了结论稳定性。
- **客观性较好的部分**：F1/F1@1/F1@2 等时序类指标不是简单依赖 LLM 判断，类别不平衡下选择 F1 作为主指标也合理。
- **潜在不足**：实验完全基于用户模拟器，缺少真实用户部署验证；部分主观指标（偏好、沟通与安全）依赖 LLM judge，可能引入评估偏差。

## 6. 主要结论与发现

- EOPA 在交互时机判断上显著超过所有基线：  
  **F1 = 30.46**，相比最强基线 Reflexion（DeepSeek-V4）提高 **19.80 个百分点**；
- Precision 达到 24.89，Recall 达到 41.10，说明在减少不必要打扰的同时能识别更多合适交互时刻；
- 在个人偏好（52.65）和沟通与安全（92.71）上也取得最佳结果，说明“时机决策与响应生成解耦”能提升响应质量；
- 推理延迟方面：静默步骤从 688ms 降至 **1.96ms**，交互步骤从 695ms 降至 **65.9ms**；
- 在线适应时间从每天 **11.41 秒降至 0.39 秒**；
- 在线动态分析显示：EOPA 在前 4 天快速上升，并从 Days 1–3 到 Days 8–14 平均提升 28.41 F1，而所有基线变化不超过 2.60；
- 部分反馈鲁棒性较好：即使只保留 50% 反馈，仍保留约 83.9% 的性能；在 25% 反馈下仍优于 Reflexion 的完整反馈结果；
- 证据可判别性随反馈积累逐步增强，同时保持低误报率；
- 在“相似状态、不同决策”任务中，EOPA 的平均相似状态决策准确率达到 38.60%，显著高于 Reflexion 的 22.72%，表明其具备更好的细粒度辨识能力。

## 7. 优点

- **问题定位清晰**：指出主动助手的关键不是“如何回应”，而是“何时主动”，并聚焦于稀疏反馈下的个性化时机问题；
- **方法设计新颖且轻量**：不使用自然语言经验或梯度训练，而是把反馈转化为可统计、可累积、可度量的上下文证据，兼顾可解释性与可扩展性；
- **保留细粒度差异**：时间锚点 + 活动原型双通道能够区分“同一时间段但不同活动”或“同一活动但不同进度”的交互需求；
- **统计稳健**：Beta-Binomial 先验平滑和不确定性缩放能缓解小样本估计波动；
- **高效实用**：时机判断完全绕过 LLM，只有交互时才调用 LLM 生成回复；在线适应开销极低；
- **实验分析较全面**：不仅给出主结果，还从消融、在线动态、效率、鲁棒性、证据质量、相似状态判别等多个角度验证方法。

## 8. 不足与局限

- **未在真实场景中验证**：整个评估基于 ProPerSim 模拟用户，真实用户对主动交互打扰的容忍度、反馈模型、活动描述噪声等都可能与模拟存在差距；
- **交互时机标签由 LLM 生成**：虽然有人工评估，但标注质量仍可能受到 LLM 偏见影响；对“合适时机”的定义可能与真实用户感受不一致；
- **主观指标依赖 LLM judge**：Personal Preference 与 Communication & Safety 使用 Qwen3.5 评判，存在 judge 偏差和稳定性问题；
- **反馈保留率下降明显**：当仅保留 25% 反馈时 F1 降至 13.35，说明方法对高度稀疏/缺失的在线反馈仍然敏感；
- **证据载体依赖手工锚定**：时间区间宽度、活动原型相似度阈值、K 值等超参数需要设定，可能影响在不同设备/场景下的泛化性；
- **活动语义质量依赖文本编码器**：如果活动视频/传感器上下文转文本的噪声较大，原型检索质量和证据可靠性可能下降；
- **论文未明确披露算力资源**，因此难以从复现角度判断整体运行成本。

（完）
