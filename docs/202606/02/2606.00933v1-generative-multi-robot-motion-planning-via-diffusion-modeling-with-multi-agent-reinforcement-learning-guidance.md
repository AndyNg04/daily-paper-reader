---
title: Generative Multi-Robot Motion Planning via Diffusion Modeling with Multi-Agent Reinforcement Learning Guidance
title_zh: 基于扩散建模与多智能体强化学习引导的生成式多机器人运动规划
authors: "Suk Ki Lee, Venkata Sai Deepak Mutta, Hyunwoong Ko"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00933v1"
tags: ["query:social-rl"]
score: 9.0
evidence: 面向多智能体合作的社会强化学习
tldr: "多机器人协调规划面临集中式难扩展、分散式忽略交互的困境。本文提出分散式扩散模型生成轨迹，结合多智能体强化学习（MARL）训练的集中价值函数，通过梯度引导反向扩散实现交互感知。在四机器人模拟迷宫中，干扰率从55.4%降至41.8%，表明MARL价值引导有效引入协调且保持可扩展性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00933v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1949, \"height\": 1663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1575, \"height\": 821, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 948, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 712, \"height\": 692, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 901, \"height\": 420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1830, \"height\": 833, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00933v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 947, \"height\": 944, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00933v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 775, \"height\": 117, \"label\": \"Table\"}]"
motivation: 解决分散式多机器人运动规划无法考虑智能体间交互导致冲突的问题。
method: 每个机器人用扩散模型独立生成轨迹，通过MARL训练的集中价值函数以梯度引导反向扩散实现交互感知。
result: "在四机器人模拟迷宫环境中，将干扰率从55.4%降低到41.8%。"
conclusion: MARL价值引导可有效协调分散式生成规划器，无需完全联合多机器人模型。
---

## 摘要
在共享环境中协调多个机器人需要为每个智能体生成可行轨迹，同时考虑智能体间的交互。集中式规划方法随着机器人数量增加而难以扩展，而允许每个智能体独立规划的分散式方法本身无法考虑智能体间的交互。本文提出一种协调多机器人运动规划的框架，该框架结合了分散式生成式轨迹规划与基于多智能体强化学习（MARL）的协调。每个机器人利用在单智能体运动数据上训练的扩散模型独立生成候选轨迹，从而发挥生成模型产生可行且多样化轨迹的能力。为减少智能体间的冲突，通过MARL训练的集中式价值函数通过基于梯度的引导来指导逆扩散过程，从而实现无需集中式联合规划或重新训练生成模型的交互感知轨迹生成。该引导遵循指数倾斜公式，其中价值函数将去噪分布偏向于具有更高期望多智能体回报的轨迹。该框架在包含四个移动机器人的模拟迷宫环境中进行评估。实验结果表明，所提出的价值引导扩散规划将智能体间干扰率从55.4%降低至41.8%，表明在保持分散式轨迹生成可扩展性的同时，可以有效实现协调。这些结果表明，基于MARL的价值引导可以有效地将协调引入分散式生成规划器，而无需完全联合的多机器人模型。

## Abstract
Coordinating multiple robots in shared environments requires generating feasible trajectories for each agent while accounting for interactions among agents. Centralized planning approaches become difficult to scale as the number of robots increases, while decentralized approaches that allow each agent to plan independently do not inherently account for inter-agent interactions. This paper presents a framework for coordinated multi-robot motion planning that combines decentralized generative trajectory planning with multi-agent reinforcement learning (MARL)-based coordination. Each robot independently generates candidate trajectories using a diffusion model trained on single-agent motion data, leveraging the generative model's ability to produce feasible and diverse trajectories. To reduce conflicts between agents, a centralized value function trained via MARL guides the reverse diffusion process through gradient-based steering, enabling interaction-aware trajectory generation without centralized joint planning or retraining of the generative model. This guidance follows an exponential tilting formulation, in which the value function biases the denoising distribution toward trajectories with higher expected multi-agent return. The framework is evaluated in a simulated maze environment with four mobile robots. Experimental results show that the proposed value-guided diffusion planning reduces the inter-agent interference rate from 55.4% to 41.8%, demonstrating that coordination can be effectively achieved while preserving the scalability of decentralized trajectory generation. These results suggest that MARL-based value guidance can effectively introduce coordination into decentralized generative planners without requiring a fully joint multi-robot model.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多机器人运动规划中，如何在保持分散式规划可扩展性的同时，有效引入智能体间的协调以降低轨迹冲突。
- **研究背景**：
  - 集中式规划方法（如MAPF）能显式建模智能体交互，但随着机器人数量增加，联合状态空间急剧膨胀，计算复杂度高，难以扩展。
  - 分散式方法允许每个机器人独立规划，具有良好的可扩展性，但无法天然考虑智能体间的交互，易产生轨迹冲突（如空间重叠、路径干扰）。
  - 生成式模型（尤其是扩散模型）能够学习多样、可行的轨迹分布，近年来在单机器人轨迹生成中表现优异，但直接扩展到多智能体场景时仍缺乏交互协调机制。
- **整体含义**：本文提出将扩散模型（用于分散式生成轨迹）与多智能体强化学习（MARL）训练的集中价值函数（用于引导协调）相结合，在无需重新训练生成模型或集中式联合规划的前提下，实现交互感知的轨迹生成，降低智能体间干扰率。

## 2. 论文提出的方法论

### 核心思想
- **两阶段框架**：
  1. **单智能体运动先验学习**：通过强化学习（PPO）在环境中收集多样轨迹数据集，训练条件扩散模型（Temporal U-Net）作为单智能体轨迹生成器。
  2. **多智能体协调引导**：在反向扩散采样过程中，利用MARL（MAPPO）训练的集中式价值函数计算梯度，对所有智能体的联合轨迹施加协调引导（指数倾斜），使采样偏向于高协调回报的轨迹。
- **关键公式与流程**：
  - **分布引导**：引导后的联合轨迹分布为 $\tilde{p}_\theta(X) \propto p_\theta(X) \exp(\lambda V_\phi(X))$，其中 $p_\theta$ 是分散式扩散模型产生的联合先验分布，$V_\phi$ 是MARL价值函数，$\lambda$ 是引导尺度。
  - **反向扩散修正**：每个智能体的去噪均值 $\mu_t^i$ 加上协调梯度项 $\lambda \Sigma_t \nabla_i$，其中 $\nabla_i$ 是全局价值函数对智能体 $i$ 轨迹分量的梯度。
  - **算法流程**（Algorithm 1）：
    1. 初始化所有智能体轨迹为高斯噪声，并通过 inpainting 强制 start/goal 边界条件。
    2. 对每个去噪时间步 $t=T,\dots,1$：
       - 各智能体并行计算去噪均值 $\mu_t^i$。
       - 计算联合轨迹 $X_t$ 的价值梯度 $\nabla_X V_\phi(X_t)$。
       - 每个智能体从修正后的分布采样 $ \tau_{t-1}^i \sim \mathcal{N}(\mu_t^i + \lambda \Sigma_t \nabla_i, \Sigma_t)$，并再次 inpainting 保持边界条件。
    3. 返回最终联合轨迹 $X_0$。

## 3. 实验设计

- **仿真环境**：
  - 单智能体数据收集与多智能体规划：自定义 $30\text{m}\times 30\text{m}$ 迷宫，内含墙壁，机器人模型为四轮 Leatherback（半径 0.254m）。
  - MARL价值函数训练：开放空间环境，四机器人沿共同目标方向形成编队（相对位置偏移）。
- **数据集**：
  - 单智能体轨迹：使用PPO策略收集 2,800 条导航轨迹（start-goal 对），每条轨迹长度 176 步。
- **对比方法**：
  - **基线（Baseline）** ：分散式扩散模型生成轨迹，无任何引导。
  - **引导（Guided）** ：相同扩散模型，但反向扩散过程中加入MARL价值函数梯度引导（$\lambda$ 调参）。
- **评估指标**：
  - **主指标**：智能体间干扰率 $R_{\text{agent}}$（两机器人距离 < 0.508m 的时间占比）。
  - **辅助指标**：障碍物干扰率 $R_{\text{obs}}$（机器人距离墙壁 < 0.254m 的时间占比）。

## 4. 资源与算力

- 文中明确提及：**NVIDIA RTX-4090 GPU**；单智能体PPO训练约 3,500 个 episode（滚动成功率曲线），扩散模型训练 **40 epochs**；MARL价值函数在开放空间训练（未提及具体步数）。
- **未明确说明**：训练总时间、GPU数量、批量大小等细节。但基于一般经验，此类规模实验可在单卡数小时内完成。

## 5. 实验数量与充分性

- **定量实验**：在四机器人迷宫场景中，每种条件（baseline vs guided）重复 **10 次**，取平均干扰率。结果表1显示 $R_{\text{agent}}$ 从55.4%降至41.8%，降低13.6个百分点。
- **定性分析**：提供了代表性轨迹对比图（图6），展示引导后轨迹空间分离度增加。
- **充分性评价**：
  - 实验覆盖了核心对比（有无引导），但**缺乏消融实验**（如引导系数 $\lambda$ 的敏感性、不同数量机器人、不同迷宫复杂度）。
  - 仅有一个迷宫场景和固定四机器人数量，**泛化性验证不足**。
  - 价值函数训练环境（开放空间）与测试环境（迷宫）存在不匹配，导致障碍物干扰率上升（从4.6%增加到7.6%），论文对此有讨论但未设计对照实验来隔离因素。
- **公平性**：基线方法和引导方法使用相同的扩散模型和相同的 start-goal 配置，比较合理。

## 6. 论文的主要结论与发现

- MARL价值引导能有效降低分散式生成轨迹的智能体间干扰（干扰率降低13.6%），表明协调信号可以在不重新训练生成模型的情况下引入。
- 协调引导通过价值梯度在反向扩散过程中施加局部修正，使轨迹在保持全局路径的同时增加空间分离。
- 引导带来的副作用：由于价值函数在开放空间训练，在迷宫环境中可能略微增加墙壁接近风险（$R_{\text{obs}}$ 从4.6%升至7.6%），表明需要环境感知的价值函数来缓解 trade-off。
- 整体证明了“生成式模型 + 推理时价值引导”是一种可行的多机器人协调范式。

## 7. 优点

1. **可扩展性强**：每个机器人独立使用同一个扩散模型，无需集中式联合规划，计算复杂度随机器人数量线性增长。
2. **零额外训练开销**：扩散模型和MARL价值函数分别预训练，推理时通过梯度引导即可实现协调，无需重新训练或微调。
3. **理论清晰**：基于指数倾斜的分布塑造方法，将协调问题转化为概率分布偏置，与分类器引导（classifier guidance）一脉相承。
4. **实验设计扎实**：使用业界常用仿真平台（NVIDIA Isaac Sim/Isaac Lab），PPO和MAPPO作为基础算法，结果可复现性高。
5. **定性分析充分**：提供了轨迹可视化，直观展示引导效果。

## 8. 不足与局限

1. **实验覆盖有限**：
   - 仅测试4个机器人，未验证更大规模（如10+机器人）的可扩展性。
   - 仅单一迷宫地图，未在不同拓扑结构环境中测试。
   - 未进行消融实验（如引导尺度 $\lambda$ 的影响、不同去噪步数等）。
2. **价值函数环境不匹配**：MARL价值函数在开放空间训练，导致在迷宫环境中引导时轨迹可能靠近墙壁，论文虽指出但未提出解决方案（如环境感知价值函数或域适应）。
3. **干扰定义局限性**：仅使用距离阈值判断干扰，未考虑实际碰撞或速度影响，可能高估或低估真实冲突。
4. **未与现有多机器人规划方法对比**：只比较了有无引导，未与集中式规划（如CBS）或其他分散式协调方法（如ORCA）进行定量对比。
5. **随机性与可重复性**：实验仅10次重复，未报告方差或置信区间，统计显著性不确定。
6. **计算资源细节缺失**：未报告训练时间、内存占用、去噪步数对规划时延的影响等工程性能指标。

（完）
