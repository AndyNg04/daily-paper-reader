---
title: RAG-driven Multi-Agent LLM Framework with Task Decomposition for Beyond 5G Auto-Configuration
title_zh: 基于RAG与任务分解的多智能体LLM框架用于超5G自动配置
authors: "İrşat Emin Sarıdaş, Onur Salan, Ali Görçin, Ibrahim Hokelek, Hakan Ali Çırpan"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01222v1"
tags: ["query:llm-info-sys"]
score: 9.0
evidence: 基于RAG的多智能体LLM框架用于5G自动配置
tldr: "大语言模型在面向意图的网络管理中易产生幻觉和结构错误。本文提出基于检索增强生成和任务分解的多智能体框架，通过语义检索确保符合标准，将复杂配置任务分解为子任务由专用智能体处理，并引入验证器进行闭环纠错。在OpenAirInterface模拟器上，该方法将配置成功率从整体方法的71.7%提升至94.4%，平均提升22.7%。该工作为B5G网络自动化配置提供了高可靠性的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01222v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1852, \"height\": 892}, {\"url\": \"assets/figures/arxiv/2606.01222v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 929, \"height\": 512}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01222v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 929, \"height\": 396}, {\"url\": \"assets/tables/arxiv/2606.01222v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 931, \"height\": 222}, {\"url\": \"assets/tables/arxiv/2606.01222v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 929, \"height\": 258}]"
motivation: 现有LLM在生成多步复杂网络配置时存在幻觉和不一致问题，需要一种结构化的纠错机制来确保配置的准确性和符合标准。
method: 采用检索增强生成管道对齐技术标准，并通过多智能体架构将任务分解为子任务，配置验证器以低计算代价识别并分段再生幻觉参数。
result: "在OpenAirInterface仿真中，配置成功率从71.7%提升至94.4%，平均成功率提升22.7%。"
conclusion: 该框架有效缓解了LLM的幻觉问题，显著提高了网络自动配置的成功率，为B5G意图驱动管理提供了可靠基础。
---

## 摘要
虽然大型语言模型通过将自然语言的人类意图转化为机器可读配置，为意图驱动的网络管理提供了有前景的路径，但在多步骤和复杂任务中，它们常常出现幻觉和结构不一致的问题。为了解决这些挑战，本文提出了一种基于检索增强和任务分解的多智能体LLM框架，用于超5G网络自动配置。该框架采用语义检索增强生成流水线，确保其输出与技术标准和供应商特定手册一致。此外，它引入了一种模块化架构，用于配置生成、闭环配置验证和网络部署，其中复杂任务被分解为由专门智能体处理的较小子任务。在该架构中，幻觉配置参数由配置验证智能体识别，并通过低计算量的分段级再生进行纠正。使用OpenAirInterface仿真器进行的性能评估实验表明，所提出的基于任务分解的配置和验证方法相比整体方法平均成功率提高了22.7%，在网络配置中实现了94.4%的成功率。

## Abstract
While Large Language Models (LLMs) offer a promising path toward intent-driven network management by translating natural language human intents into machine-readable configurations, they often suffer from hallucinations and structural inconsistencies in multi-step and complex tasks. To address these challenges, this paper proposes a retrieval-augmented and task decomposition-based multi-agent LLM framework for Beyond 5G network auto-configuration. The framework employs a semantic retrieval-augmented generation pipeline to ensure that its outputs are aligned with technical standards and vendor-specific manuals. Furthermore, it introduces a modular architecture for configuration generation, closed-loop configuration verification, and network deployment, in which complex tasks are decomposed into smaller sub-tasks handled by specialized agents. In this architecture, hallucinated configuration parameters are identified by the configuration verifier agent and corrected through low computational segment-level regeneration. The performance evaluation experiments with the OpenAirInterface emulator demonstrate that the proposed task decomposition-based configuration and verification approach improves the average success rate by 22.7% over monolithic methods, achieving 94.4% success in network configuration.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：Beyond 5G（B5G）网络环境日益异构，配置和运营管理高度复杂且依赖专家知识。大语言模型（LLM）有望通过将自然语言意图转化为机器可读配置，实现意图驱动的网络自动化管理。
- **核心问题**：LLM 在多步骤、严格依赖的配置任务中容易出现**幻觉**（生成不存在或不一致的参数）和**结构不一致**，导致生成的配置不符合技术标准（如 3GPP）或平台规范，难以直接部署。
- **研究动机**：现有方法（如单次提示或整体生成）缺乏可靠的纠错机制，且对标准/手册的依赖不足。需要同时解决**领域知识注入**和**结构化推理**两个关键挑战。
- **整体含义**：本文旨在提出一种结合检索增强生成（RAG）和任务分解的多智能体 LLM 框架，将人类意图转化为可靠、可部署的 B5G 网络配置，为意图驱动的零接触网络管理提供可行方案。

## 2. 方法论：核心思想、关键技术细节与算法流程

- **核心思想**：通过**任务分解**将复杂配置拆分为多个逻辑子任务，由**专用智能体**分别生成和验证；通过**RAG 管道**注入标准知识，减少幻觉；通过**闭环自精简**（验证-反馈-选择性再生）逐步修正错误，直至全部合规。
- **关键技术细节**：
  - **整体架构**（见图 1）：部署于服务管理与编排（SMO）平台，包含多智能体编排器、三个主要智能体：
    - **配置生成智能体（CGA）**：接收用户意图，通过 RAG 检索相关模板和参数规则，分解为子任务（如 gNB 的 CU/DU/RU 参数段），由段生成智能体并发执行，最后合并为完整配置。
    - **配置验证智能体（CVA）**：按相同分解逻辑将配置分段，逐段验证（结构、语义、依赖关系），生成 JSON 格式评估报告，仅将错误段打回再生，有效段保留重用。
    - **网络部署智能体（NDA）**：基于验证通过的配置生成部署命令（Shell 脚本），通过**人机协同**（human-in-the-loop）用户审批后方才执行。
  - **RAG 管道**：离线将示例配置文和基础参数知识（含定义、数据类型、范围、依赖关系等）向量化（使用 all-MiniLM-L6-v2 嵌入），运行时对用户意图执行 top-k 近邻检索，经后过滤后注入提示中。
  - **任务分解策略**：静态分解，依据功能模块（如 gNB 的 CU/DU/RU、CN 的各功能）划分；验证阶段仅对错误段执行选择性再生，降低计算开销。
  - **闭环流程**：CGA 生成 → CVA 验证 → 若有错误则反馈 CGA 再生错误段 → 重新验证，直至全部通过 → 移交 NDA 部署。
- **公式/算法（文字说明）**：无数学公式，但算法流程可描述为：
  1. 用户输入自然语言意图；
  2. 语义 RAG 检索相关上下文；
  3. CGA 将任务静态分解为域级（CN/gNB/UE）→ 域内再分解为段级；
  4. 各段生成智能体并发生成各自配置段；
  5. 合并为完整配置；
  6. CVA 按同样分解路径分段验证；
  7. 若某段错误，则将评估报告（含错误原因和修正建议）送给 CGA 再生该段；
  8. 重复步骤 6-7 直至所有段通过；
  9. NDA 生成部署命令，用户审批后执行。

## 3. 实验设计

- **数据集/场景**：
  - 使用 **OpenAirInterface (OAI) 5G 仿真器**作为目标网络平台，搭建端到端私有 5G 网络。
  - RAG 知识库包含 37 个 gNB 配置文件和 5 个 UE 配置文件（来自 OAI 官方仓库）。
  - 测试仅针对 **gNB 配置生成与部署**（CN 和 UE 使用默认配置）。
- **提示生成**：基于真实配置参数池，为每个提示随机选取 5 个参数并赋值，构造自然语言配置请求，确保多样性且反映真实约束。
- **对比方法**（四种策略）：
  - **MG (Monolithic Generation)**：单次 RAG 增强整体生成，无验证。
  - **DG (Decomposed Generation)**：任务分解生成，无验证。
  - **MGMV (Monolithic Generation + Monolithic Verification)**：整体生成 + 整体验证。
  - **DGDV (Decomposed Generation + Decomposed Verification)**：分解生成 + 分解验证（本文完整方法）。
- **评价指标**：**部署成功率**（Success Rate）：生成的配置能成功部署（gNB 无错误启动）且符合用户意图的比例。
- **实验规模**：每个方法使用 **10 个不同提示**，每个提示生成 **100 个配置**，共 **2000 个 gNB 配置**（原文描述“1000 configurations per strategy and 2000 generated gNB configurations in total”，可能指生成阶段（MG+DG）共 2000 个，验证阶段基于这些配置进一步评估）。四个方法均覆盖。

## 4. 资源与算力

- **硬件平台**：Supermicro ARS-111GL-NHR 服务器，配 72 核 ARM CPU，**NVIDIA H100 GPU**（用于 LLM 推理）。
- **软件栈**：Ubuntu Server 22.04、Ollama 平台、GPT-OSS-120B 模型（1200 亿参数）、LangChain 编排、FAISS 向量检索、all-MiniLM-L6-v2 嵌入。
- **LLM 推理配置**：温度参数设为 0.3，在确定性和灵活性间平衡。
- **训练信息**：**未提及模型训练**，仅使用预训练模型进行推理。实验时间为离线推理，未报告训练时长。
- **推理延迟**：表 II 给出各阶段的平均时间（秒）：
  - MG 生成：18.274，MG 验证：27.612，整体总计：45.886
  - DG 生成：47.055，DV 验证：32.905，分解总计：79.961
  - 分解方法延迟增加约 74%（45.9s → 80.0s）。

## 5. 实验数量与充分性

- **实验数量**：
  - 共 4 种方法 × 10 个提示 × 100 次生成 = **4000 次**生成/验证尝试（但配置生成总数原文写 2000，可能是生成阶段两个方法各 1000，验证阶段复用；总体上实验覆盖充分）。
  - 提供了消融分析：对比 MG vs DG（生成阶段）、MG vs MGMV（验证对整体的影响）、MGMV vs DGDV（分解 vs 整体验证）。
  - 提供了错误类型分布（表 III）和推理时间统计（表 II）。
- **充分性评价**：
  - **充分**：10 个提示覆盖多种参数组合，100 次重复提供统计学可靠性。
  - **公平**：所有方法使用相同提示集、相同 RAG 知识库、相同 LLM 和超参数。
  - **客观**：成功与否由实际 OAI 部署测试决定，非主观判断。
  - **不足**：仅测试一个 LLM（GPT-OSS-120B），未跨模型泛化；仅测试 gNB 配置，未验证 CN 和 UE 端；知识库来自单一开源平台（OAI），未覆盖多供应商。

## 6. 主要结论与发现

- **性能提升**：所提 **DGDV 方法平均成功率达到 94.4%**，相较于整体方法（MG 或 MGMV?）提升 **22.7%**。
- **分解 vs 整体**：单看生成阶段，DG 比 MG 平均成功率高 15.4%（图 2 avg 约 86%→90%），但耗时增加。
- **验证效果**：加上分解验证后，DGDV 相比 DG 进一步大幅提升，且验证能恢复大量初期失败（尤其在 Prompt 7 等困难场景中表现显著）。
- **错误分布**（表 III）：多数失败由“无效值”引起；DG 中“重复参数”和“语法错误”占比相对较高，说明分解可能引入新类型错误，但验证能有效抑制。
- **延迟-可靠性权衡**：分解方法虽然推理时间增加约 74%，但可靠性显著提升，在关键网络部署场景中可接受。
- **互补性**：任务分解和闭环验证是**互补机制**，结合使用获得最佳效果。

## 7. 优点

- **方法创新性**：首次将 **RAG + 任务分解 + 多智能体 + 闭环验证** 系统化应用于 B5G 自动配置，有效缓解幻觉问题。
- **模块化设计**：CGA、CVA、NDA 可独立使用，适配不同运营商需求；分解粒度灵活。
- **计算高效**：分段验证 + 选择性再生避免整体重做，节省 token 和计算。
- **实用性强**：采用真实开源仿真器 OAI 和 GPT-OSS-120B，部署命令经人机协同执行，兼顾安全。
- **实验设计规范**：消融分析清晰，每次实验重复 100 次，统计稳定；使用固定 RAG 知识库和提示集保证公平。

## 8. 不足与局限

- **部署环境单一**：仅测试了 OAI 仿真器，未在真实硬件或商业基站验证；知识库仅来自 OAI 官方配置，未包含多供应商手册。
- **模型泛化性不足**：仅评估了 GPT-OSS-120B，未对比其他 LLM（如 GPT-4、Llama 等），无法确定模型选择对结果的影响。
- **任务分解静态**：当前按固定功能模块划分（CU/DU/RU），未实现 LLM 自主动态分解，可能不适应不同网络拓扑。
- **提示合成偏差**：提示由 LLM 基于参数池合成，虽增加多样性但可能不完全模拟真实用户表述；未使用真实运营商需求测试。
- **延迟成本**：分解方法推理时间翻倍（45.9s→80.0s），在实时性要求高的场景可能受限；文中未讨论优化方案（如并行加速）。
- **缺少可解释性分析**：未分析 LLM 给出错误修正建议的准确性，也未对验证器的误报警/漏报进行量化。
- **域限制**：实验仅覆盖 gNB 配置，未完整验证 CN 和 UE 的功能；RAG 知识库仅 37+5 个配置，规模较小。

（完）
