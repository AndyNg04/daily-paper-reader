---
title: "Structures Facilitate Retrieve, Rerank, and Generate"
title_zh: 结构助力检索、重排序与生成
authors: "Yeqin Zhang, Haomin Fu, Xujie Zhang, Cam-Tu Nguyen"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03247v1"
tags: ["query:llm-info-sys"]
score: 9.0
evidence: 结构增强的检索-重排序-生成方法用于RAG
tldr: 文档式对话系统需从外部文档中检索信息来回答用户问题。现有方法将文档切分为独立段落，忽略了文档内部结构信息，导致检索和生成缺乏上下文。本文提出SF-Re2G方法，首先通过同节内段落对比学习改进表示以提高检索；然后利用结构信息将候选段落分组，在重排序时融入组信息；最后生成时考虑子图上下文。在英文和中文数据集上实验验证了方法的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03247v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1247, \"height\": 426, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03247v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1245, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03247v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1255, \"height\": 475, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03247v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 682, \"height\": 440, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03247v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1133, \"height\": 435, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03247v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1080, \"height\": 377, \"label\": \"Table\"}]"
motivation: 现有方法将文档切分为独立段落，忽略了文档结构信息，导致检索和生成缺乏上下文。
method: 通过同节段落对比学习改进表示，利用文档结构将候选分组进行重排序，并在生成时融入子图上下文。
result: 在英文和中文两个文档式对话数据集上取得了优于基线方法的性能。
conclusion: 利用文档结构信息能有效提升文档式对话系统的检索、重排序和生成质量。
---

## 摘要
基于文档的对话系统（DGDS）利用外部文档中的知识来回答特定领域的用户问题。现有解决方案通常将文档划分为独立的段落进行检索和响应生成。然而，这种方法既没有充分利用文档内的结构信息，也没有为知识选择和响应提供足够的（文档）上下文。本文提出SF-Re2G来系统性地解决这些问题。首先，我们通过将某段落与同一部分的其他段落进行对比来改进段落表示，从而提升检索性能。其次，构建了一个结构增强的重排序器，利用一个对话回合的多个依据段落往往位于同一邻域这一事实。具体来说，根据文档结构将检索得到的候选段落分组为子图。重排序器将结合组信息对候选段落重新评分。最后，将选中的段落用于响应生成，同时考虑子图上下文以实现更好的生成效果。在两个DGDS数据集上的实验结果验证了我们的方法在中文和英文上的有效性。

## Abstract
Document-grounded dialogue systems (DGDS) utilize knowledge from external documents to answer domain-specific user questions. Existing solutions typically divide documents into independent passages for retrieval and response generation. This approach, however, neither makes good use of structural information within documents nor provides enough (document) context for knowledge selection and responses. This paper proposes SF-Re2G to address such issues systematically. Firstly, we seek to improve a passage representation by contrasting it with others of the same section, thus improving the retrieval performance. Secondly, a structure-enhanced reranker is built, leveraging the fact that multiple grounding passages of one dialog turn tend to be in the same neighborhood. Specifically, candidates from the retrieval are grouped into subgraphs according to the document structure. The reranker will rescore the candidate integrating its group information. Finally, the chosen passages are used for responses, taking into account the subgraph context for better generation. Experimental results on two DGDS datasets validate our method for both Chinese and English.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：文档式对话系统（DGDS）依赖外部文档回答特定领域的用户问题。现有方法（如RAG、Re2G）将文档切分为独立段落进行检索和生成，忽略了文档内部的**结构信息**（如章节、表格、树状层级），导致两个问题：①段落粒度难以权衡（长段落含噪声，短段落缺乏上下文）；②无法利用同一文档内相关段落间的语义关联，造成检索和生成质量次优。
- **整体含义**：本文首次系统性探索文档结构对DGDS全流程（检索、重排序、生成）的增强作用，提出SF-Re2G方法，证明结构信息能显著提升系统性能。

## 2. 方法论：核心思想、关键技术、流程
- **核心思想**：将文档建模为文档图（Document Graph），节点为段落；利用图结构在检索、重排序、生成三个阶段融合结构信息。
- **关键技术**：
  - **检索阶段**：
    - 采用Bi-encoder（双编码器）架构，将对话上下文和段落分别编码为向量，通过点积计算相似度。
    - **结构对比学习**：从文档结构中采样**硬负样本**（即与正样本属于同一章节的邻居段落），替换传统的BM25负样本，使模型能更好区分语义相近的段落。
    - 使用Top-Down聚合（借鉴TreeJC）将上层信息（如标题）融入段落表示。
  - **重排序阶段**：
    - 利用Cross-encoder（交叉编码器）对检索得到的top-k段落进行精细评分。
    - **结构分组**：将候选段落按文档结构分组为子图（同一文档内深度优先遍历，限制输入长度），输入时组合核心段落与同组其他段落，模型根据组信息重新打分。
    - 损失函数与检索阶段相同（负对数似然）。
  - **生成阶段**：
    - 采用加权融合方式：生成概率 = 重排序得分 × BART解码概率，其中生成器输入包含对话历史、检索段落及其子图上下文。
    - 训练时联合优化重排序器和生成器（端到端）。
- **算法流程**（文字说明）：
  1. 构建文档图G，节点为段落。
  2. 检索：编码对话上下文和每个段落，用结构对比学习训练检索器，输出top-k段落。
  3. 重排序：将top-k段落按文档结构分组为子图，输入Cross-encoder，输出结构增强得分。
  4. 生成：取top-k段落及其子图，以得分为权重，由BART生成最终响应。

## 3. 实验设计
- **数据集**：
  - **MultiDoc2Dial**（英文）：来自4个领域（ssa、va、dmv、student），训练集约48002 utterances，评估集约9195 utterances。
  - **Doc2Bot**（中文）：来自4个领域（health、tech、insurance、wikihow），训练集约11485 utterances，评估集约2941 utterances。
  - 两个数据集覆盖不同语言和结构复杂度，验证跨语言能力。
- **基准方法**：
  - 检索阶段对比：BM25、DPR（无结构增强）、DPR_struct（加入Top-Down聚合作为强基线）。
  - 重排序阶段对比：CrossEncoder（无结构分组）、CrossEncoder_struct（结构增强）。
  - 生成阶段对比：RAG、Re2G、Re2FiD（使用Fusion-in-Decoder替换生成器）。
- **评价指标**：
  - 检索：Recall@1/5/10/100。
  - 重排序：Recall@1/5/10、Rprec。
  - 生成：F1、SacreBLEU、ROUGE。

## 4. 资源与算力
- 论文中**未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅提及实现基于PyTorch和HuggingFace Transformers，预训练模型包括BERT、RoBERTa、BART。未提供训练时间或计算开销分析。

## 5. 实验数量与充分性
- **实验组数**：至少包含3组主要实验：①检索消融实验（表2），②重排序消融实验（表2），③生成对比实验（表3）。此外，生成阶段还包含子图消融（w/o subgraph）。
- **充分性评价**：
  - 优点：覆盖两个不同语言、不同结构复杂度的数据集；在检索、重排序、生成三个阶段均进行消融；与多个SOTA方法（RAG、Re2G、Re2FiD）对比。
  - 不足：仅对比了3种生成基线方法，未包含更多近期模型（如基于LLM的方法）；未报告不同领域的独立结果（可能遮挡领域差异）；未进行统计显著性检验。

## 6. 主要结论与发现
- **结构信息提升检索**：基于文档结构采样的硬负样本比BM25负样本更有效，在MultiDoc2Dial上Recall@1提升约2%，Doc2Bot上提升约1%。
- **结构分组提升重排序**：在Doc2Bot上结构增强重排序器提升约1%指标，但MultiDoc2Dial上提升不明显（推测因该数据集结构较弱）。
- **生成阶段：子图上下文的双面性**：在Doc2Bot（结构丰富）上添加子图明显提升生成性能；在MultiDoc2Dial（结构简单）上移除子图反而更好，说明结构信息需与数据集特性匹配。
- **整体SOTA**：SF-Re2G在多个指标上达到或超越现有方法，且使用比Re2FiD更少的参数（仅单解码器）。

## 7. 优点
- **系统性**：首次将结构信息集成到DGDS完整的“检索-重排序-生成”流水线，每个阶段都有针对性设计。
- **创新性**：①结构对比学习用于硬负采样；②结构分组用于重排序；③子图上下文用于生成加权。
- **跨语言验证**：在英文（MultiDoc2Dial）和中文（Doc2Bot）数据集上均有效，表明方法具有语言普适性。
- **高效性**：生成器使用BART（单解码器）即达到甚至超过双解码器Fusion-in-Decoder的性能。

## 8. 不足与局限
- **实验覆盖局限**：仅使用两类结构（树形文档和表格），未测试其他结构类型（如嵌套列表、多级标题）；未评估在开放域或高度结构化的知识图谱上的表现。
- **结构依赖风险**：方法有效性高度依赖文档结构的质量与完整度。对于结构不清晰或随意分段的文档，优势可能消失（如MultiDoc2Dial上的部分结果所示）。
- **计算开销**：重排序阶段使用Cross-encoder对分组子图进行多次前向传播，可能增加推理延迟，论文未分析时间效率。
- **偏差风险**：实验仅在两个基准数据集上进行，且生成阶段仅对比了3种方法，可能遗漏更优模型（如基于大语言模型的方法）。
- **消融不完整**：未单独分析Top-Down聚合对检索的贡献，也未探索不同分组策略对重排序的影响。
- **可复现性**：未提供代码或超参数详细列表（仅提及“参考补充材料”，但补充材料未在本文中完整呈现）。

（完）
