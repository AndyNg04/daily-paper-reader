---
title: Personalizing Embodied Multimodal Large Language Model Agents over Long-term User Interactions
title_zh: 基于长期用户交互的具身多模态大语言模型智能体个性化
authors: "Jeongeun Lee, Chanyoung Park, Dongha Lee"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26256v1"
tags: ["query:human-ai"]
score: 9.0
evidence: "利用长期用户交互实例化具身AI智能体, 增强人机协作"
tldr: "该论文指出具身AI智能体在个性化服务中难以仅凭当前指令推断隐含目标, 提出POLAR框架, 将长期用户交互组织为多模态知识图谱, 融合语义记忆、情景记忆和视觉概念, 使智能体能够利用累积的个性化上下文进行推理, 在真实场景的实验中验证了其有效性, 为设计能与用户长期协作的AI助手提供了新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26256v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1426, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2605.26256v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1422, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2605.26256v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1397, \"height\": 561}, {\"url\": \"assets/figures/arxiv/2605.26256v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1439, \"height\": 611}, {\"url\": \"assets/figures/arxiv/2605.26256v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1405, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2605.26256v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 455, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2605.26256v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 454, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2605.26256v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 456, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2605.26256v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1421, \"height\": 596}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.26256v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1425, \"height\": 809}, {\"url\": \"assets/tables/arxiv/2605.26256v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 525, \"height\": 249}]"
motivation: "利用长期用户交互实例化具身AI智能体, 增强人机协作。"
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
基于多模态大语言模型（MLLM）的具身智能体在解决物理环境中的复杂任务方面展现出强大潜力。然而，个性化辅助需要的不仅仅是遵循通用指令或识别物体类别。在真实场景中，目标通常仅通过先前的交互隐式指定，这就要求智能体利用随时间积累的个性化上下文。在本文中，我们提出了POLAR，一个基于多模态记忆增强的框架，用于在长期用户交互中实现个性化具身智能体。POLAR将先前的交互组织成多模态知识图谱，其中包含用于个性化上下文和视觉概念的语义记忆，以及用于具身体验（如智能体轨迹）的情景记忆。为了执行具身任务，POLAR检索相关记忆来解释当前请求并指导任务执行。我们在多个MLLM骨干网络和多样化的评估场景下评估了POLAR，以研究记忆在长期个性化中的作用。结果表明，所提出的记忆机制通过更有效地利用先前交互中积累的信息，持续提升了性能。当智能体需要在多个交互间进行推理、执行多跳推理，或跟踪用户特定上下文随时间更新时，这种提升尤为显著。

## 速览
**TLDR**：现有具身多模态大语言模型代理难以利用长期用户交互中的隐含个性化需求。POLAR框架将历史交互组织为多模态知识图，结合语义与情景记忆，检索相关记忆以指导任务执行。实验显示该方法在多个基座模型上持续提升性能，尤其在多交互推理和多跳推断场景下效果显著，表明记忆增强是实现个性化具身代理的有效途径。 \
**Motivation**：具身代理在真实世界中需要基于用户历史交互的隐含意图进行个性化执行，但传统方法缺乏对长期上下文的有效利用。 \
**Method**：提出POLAR，将先前交互构建为多模态知识图，同时存储语义和情景记忆，通过检索相关记忆来辅助任务推理与执行。 \
**Result**：在多个MLLM基座和多样化场景中，POLAR的记忆机制一致提升了性能，尤其在需要跨交互推理或多跳推断时增益更明显。 \
**Conclusion**：POLAR验证了长期记忆对个性化具身代理的重要性，为多模态LLM代理的持久上下文建模提供了新框架。

---

## Abstract
Multimodal large language model (MLLM)-based embodied agents have shown strong potential for solving complex tasks in physical environments. However, personalized assistance requires more than following generic instruction or recognizing object categories. In real-world scenarios, the intended target is often specified only implicitly through prior interactions, requiring agents to leverage personalized context accumulated over time. In this work, we propose POLAR, a multiomodal memory-augmented framework for personalized embodied agents over long-term user interactions. POLAR organizes prior interactions into a multimodal knowledge graph that captures semantic memory for personalized context and visual concepts, and episodic memory for embodied experiences such as agent trajectories. To execute embodied tasks, POLAR retrieves relevant memories to interpret the current request and guide task execution. We evaluate POLAR across multiple MLLM backbones and diverse evaluation scenarios to study the role of memory in long-term personalization. Results show that the proposed memory mechanism consistently improves performance by enabling more effective use of information accumulated over prior interactions. The gains are especially pronounced when the agents are required to reason across multiple interactions, perform multi-hop inference, or tracking updates in user-specific context over time.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：现有的多模态大语言模型（MLLM）具身智能体通常依赖类别级的物体识别和显式指令，但在真实个性化服务中，用户往往通过长期交互隐式地指定目标（如“帮我把我的鞋拿来”）——环境中可能存在多个同类的“鞋”，智能体必须从先前的对话与行动中推断出用户意图的具体实例。
- **核心问题**：**长期用户交互下的个性化实例定位与规划**。智能体需要从若干段历史交互中提取、组织并推理用户专属的上下文，才能准确完成任务，而非仅依赖当前观察或原始对话记录。
- **整体含义**：提出**记忆增强的框架 POLAR**，将先前的交互转化为结构化的多模态知识图谱，使智能体能够像人类一样利用个性化记忆来执行当前任务，弥补了现有 MLLM 在长上下文推理和用户适配性方面的不足。

### 2. 论文提出的方法论
- **核心思想**：**多模态记忆增强的个性化具身智能体**
  - 将长期用户交互记录组织为**对象为中心的知识图谱**，分为语义记忆（个性化属性）和情景记忆（过往轨迹经验）。
  - 执行任务时，通过检索器从图中获取与当前指令相关的候选对象记忆，再交给 MLLM 进行实例定位和动作规划。
- **关键技术细节**：
  - **记忆构建（Memorization）**：
    - 每个对象节点（Object Node）对应一个物理实例（由 ID 或视觉相似度关联）。
    - **语义记忆节点**：将每个交互中关于该对象的用户专属信息拆解为多个简洁的陈述句（如“这双鞋是送给女儿的礼物”“上次放在衣柜旁”），避免单条记忆混杂过多信息。
    - **情景记忆节点**：从原始轨迹中提取与规划相关的经验（成功/失败、哪些房间无效、关键观察点等），生成紧凑的文本摘要 `p_i`。
    - 新记忆通过 ID 匹配或视觉相似度链接到已有对象节点；若语义记忆高度重复，则复用已有节点。
  - **记忆利用（Utilization）**：
    - 对当前指令 `I` 计算与所有语义记忆节点的相似度（使用 BGE-M3 编码器），取 top-k（k=5）最相似的语义节点。
    - 沿图连边找到对应对象节点，收集其关联的指令和情境记忆，形成候选池 `R_k`。
    - MLLM 体规划策略：`a_t = π(τ_t, I_k, R_k)`，即在观察历史、指令和检索记忆的融合下预测下一步动作。
  - **行动空间设计**：分层架构
    - 高层规划：先生成场景图，推理全局空间结构，输出前往某房间的航点序列。
    - 低层规划：从第一视角图像中选择 `MOVE FORWARD`, `TURN LEFT/RIGHT`, `STOP` 等基本动作。
- **公式与流程**：问题建模为 POMDP，每步行动依赖历史 `τ_t`、指令 `I_k` 和检索记忆 `R_k`。整体流程为 `E_{<k} → M → R_k = ρ(I_k, M) → a_t`。

### 3. 实验设计
- **数据集与场景**：
  - 构建在 **PinNED** 数据集之上，使用 Habitat 模拟器中的 **Habitat-Matterport3D (HM3D)** 场景，包含 4 个真实房屋场景、1817 个 episode。
  - 为每个目标对象生成显式个性化上下文（作为“习得阶段”）和隐式评测指令（无明确对象引用），两者位置和起点不同，防止轨迹复用。
- **评测场景**：
  - **组合场景（Compositional）**：分为单交互（single）和联合多交互（joint），后者需要跨多个 episode 组合信息。
  - **干扰场景（Distractor）**：环境中有多个同类别候选对象，测试实例级区分能力。
  - **时间场景（Temporal）**：个性化上下文随时间更新（属性改变或同一描述对应不同对象），考验使用最新有效交互的能力。
- **对比方法**：
  - **no-prior**：仅用当前指令和观察。
  - **raw-interaction**：从先前交互中随机采样 15 条原始记录（含黄金交互）作为附加输入。
  - **POLAR**：本框架。
- **基座 MLLM**：开源模型 Qwen3-VL-8B, Qwen2.5-VL-8B；闭源模型 GPT-5, GPT-4o-mini, Gemini-2.5-Flash。
- **评估指标**：
  - **成功率 (SR)**：最终位置距目标 < 2 米。
  - **路径加权成功率 (SPL)**：兼顾效率。
  - 附加指标：**类别匹配 (CM)**（同类别误中实例数）和检索召回率。

### 4. 资源与算力
- **算力说明**：
  - 论文**未明确提及**训练 GPU 型号、数量或训练时长。因 POLAR 是一个无训练的检索增强框架，不需要训练模型参数，仅调用 MLLM 和编码器（BGE-M3）进行推理。
  - 数据集生成使用了 GPT-5（两次过滤：人工 + MTurk 验证），但计算资源消耗未量化。
  - 实验主要在仿真环境 Habitat 中运行，最大步数 700 步，无参数优化部分，因此主要算力来自 MLLM 推理的 API 调用或本地部署。

### 5. 实验数量与充分性
- **实验组合**：
  - 5 种 MLLM 基座 × 3 种情境（compositional 分 single/joint、distractor、temporal） × 3 种方法（no-prior, raw-interaction, POLAR）。表中提供了详尽的 SR 和 SPL。
- **辅助分析**：
  - 习得阶段与评测阶段性能差距对比（图 5）。
  - 类别误匹配 (CM) 对比（图 6）。
  - 控制检索下的成功率对比（图 7，POLAR vs. raw-interaction 都获黄金信息时）。
  - 检索召回对比（图 8，语义节点 vs BM25 vs 直接密集检索）。
  - 情景记忆表示消融：仅指令、原始轨迹、摘要、POLAR 情节记忆（表 2）。
- **充分性**：实验覆盖多基座、多维度场景，消融实验揭示了组件贡献，并通过 CM 和检索分析深化了定性理解，整体设计全面、对比公平。

### 6. 论文的主要结论与发现
- **仅有当前信息不够**：no-prior 性能极低，表明单次观测无法完成个性化实例定位。
- **原始交互不稳定**：raw-interaction 虽能引入用户信息，但因非结构化、噪声大，表现波动且不可靠。
- **POLAR 显著提升**：转换为对象中心的图记忆后，性能在所有场景和基座上均一致提升，尤其是在需要跨多交互联合推理或跟踪上下文更新的 temporal/joint 场景。
- **语义记忆改造检索**：将交互拆解为细粒度陈述，比直接检索原始对话大幅提高任务相关信息的召回率。
- **情景记忆优于原始轨迹**：提取的“规划相关”经验比完整轨迹或简易摘要更有效，能提供可操作的搜索线索。
- **个性化影响全栈**：不仅改善目标定位，也影响后续的空间搜索策略（如图 9 案例所示）。

### 7. 方法的优点
- **结构化记忆设计**：将长期交互从非结构化日志转为对象驱动的知识图谱，具备可检索性、可更新性和可组合性。
- **模块解耦**：记忆构建和利用分离，无需微调 MLLM，适用于任意强大的多模态模型。
- **双重记忆类型**：语义记忆处理“什么”的对象属性，情境记忆总结“怎么找”的导航经验，互补力强。
- **实验严谨**：覆盖多种先进 MLLM、多类评测场景，提供了细粒度的控制实验和消融分析。
- **开源性潜力**：基于 Habitat 和 PinNED 等开放平台，易于复现和扩展。

### 8. 不足与局限
- **任务单一**：目前仅验证在导航任务上，未涉及抓取、操作等更复杂的具身交互。对通用机器人管家的泛化能力有待检验。
- **环境限制**：实验在 4 个 HM3D 仿真场景中进行，真实世界部署会面临更复杂的视觉和力学动态。
- **记忆初始化依赖**：构建知识图谱时需预先识别对象或匹配视觉特征；在完全未知环境中增量构建的效果尚不明确。
- **上下文生成质量**：依赖 GPT-5 生成个性化上下文，可能引入风格偏差或难以覆盖真实用户的多样表达。
- **计算效率未评估**：检索和图更新的推理开销未报告，长期交互积累大量节点时的性能退化未分析。
- **隐私与安全**：个性化记忆存储用户专属信息，但未讨论隐私保护或用户控制机制。

（完）
