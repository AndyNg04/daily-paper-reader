---
title: In-Context Reward Adaptation for Robust Preference Modeling
title_zh: 面向鲁棒偏好建模的上下文奖励自适应
authors: "Zhenyu Sun, Zheng Xu, Ermin Wei"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30323v1"
tags: ["query:social-rl"]
score: 10.0
evidence: 针对RLHF中多样人类偏好的鲁棒偏好建模
tldr: 现有RLHF依赖静态奖励模型，难以泛化到多样且未见的偏好域。本文提出基于Transformer的上下文奖励自适应框架，通过少量偏好演示在线推断奖励结构。引入人类反应时间作为辅助信号，克服了标准架构的渐近偏差，成功适应未见域的偏好。该方法为异构奖励建模与偏好分布偏移提供了稳健基础，推动更灵活的人机对齐。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30323v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 966, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30323v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30323v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 919, \"height\": 300, \"label\": \"Table\"}]"
motivation: 单一奖励模型缺乏鲁棒性，多奖励框架受限于固定域，无法以低成本适应未见的人类偏好分布。
method: 利用Transformer上下文学习，从少量偏好演示中自适应推理奖励，并引入反应时间辅助信号消除估计偏差。
result: 模型成功适应未知偏好域，克服了标准架构的渐近偏差，实现了对异构奖励和分布偏移的表示。
conclusion: 所提方法提供了更稳健的偏好建模基础，能灵活应对偏好变化，为人机对齐指出了一条可扩展路径。
---

## 摘要
基于人类反馈的强化学习（RLHF）通常依赖静态奖励模型来使大语言模型与人类偏好对齐。然而，人类价值观本质上是多样且异质的，单一奖励模型往往缺乏泛化到未见过的偏好领域所需的鲁棒性。现有的多奖励框架试图解决这一问题，但它们通常局限于一组固定的已知领域，无法在没有昂贵重新训练的情况下适应未见的人类分布。在这项工作中，我们提出了上下文奖励自适应，这是一个基于Transformer的框架，旨在动态地建模多样且未见的人类偏好。通过利用Transformer的上下文学习能力，我们的方法从少量偏好示例中自适应地推断出潜在的奖励结构。我们证明，标准的Transformer架构不足以完成此任务，因为它对真实情况存在渐近偏差；而通过引入人类响应时间作为辅助输入信号，模型能够成功地适应来自先前未见领域的偏好。我们的发现表明，这种方法为偏好建模提供了更鲁棒的基础，允许表征异质奖励和偏好分布偏移，并为更灵活的人类-AI对齐提供了可扩展的途径。

## 速览
**TLDR**：传统基于人类反馈的强化学习（RLHF）依赖静态奖励模型，难以泛化至多样且异质的人类价值偏好，现有方法无法适应未见的偏好领域。为此，本文提出上下文奖励自适应（In-Context Reward Adaptation）框架，利用Transformer的上下文学习能力，仅从少量偏好演示中推断底层奖励结构。研究发现，单纯使用标准Transformer存在渐近偏差，而融入人类响应时间作为辅助信号后，模型成功适应来自未见领域的偏好。该工作为偏好建模提供了更稳健的基础，能够表征异构奖励和应对偏好分布偏移，为可扩展的人机对齐开辟了灵活路径。 \
**Motivation**：现有RLHF奖励模型静态单一，无法处理多样、异构且未见的偏好，需灵活自适应方法。 \
**Method**：基于Transformer的上下文奖励自适应框架，利用偏好示例和响应时间信号动态推断奖励结构。 \
**Result**：融入人类响应时间后，成功适应未见偏好域，克服了标准Transformer的渐近偏差。 \
**Conclusion**：该方法为异构偏好建模和分布偏移提供稳健基础，开拓了可扩展的灵活人机对齐路径。

---

## Abstract
Reinforcement Learning from Human Feedback (RLHF) typically relies on static reward models to align Large Language Models with human preferences. However, human values are inherently diverse and heterogeneous, and a single reward model often lacks the robustness required to generalize to unseen preference domains. While existing multi-reward frameworks attempt to address this, they are often restricted to a fixed set of known domains and fail to adapt to unseen human distributions without costly retraining. In this work, we propose In-Context Reward Adaptation, a transformer-based framework designed to model diverse and unseen human preferences on the fly. By leveraging the in-context learning capabilities of transformers, our approach adaptively infers the underlying reward structure from a small set of preference demonstrations. We demonstrate that while a standard transformer architecture is insufficient for this task by characterizing an asymptotic bias to the ground-truth, incorporating human response time as an auxiliary input signal enables the model to successfully adapt to preferences from previously unseen domains. Our findings show that this approach provides a more robust foundation for preference modeling, allowing for the representation of heterogeneous rewards and preference distribution shift, and offering a scalable path toward more flexible human-AI alignment.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究问题**：现有 RLHF 通常采用单个静态奖励模型，忽略了人类偏好的多样性和异质性。多奖励模型虽试图建模异质偏好，但局限于固定的已知域，无法以低成本自适应未见或动态变化的偏好分布。
- **核心挑战**：如何在推理时从少量偏好示例中自适应地推断出未知的人类奖励结构，而无需重新训练。
- **整体含义**：提出一种基于 Transformer 的上下文奖励自适应框架，并揭示仅使用二值偏好标签的模型存在渐近偏差，引入反应时间作为辅助信号后可以消除该偏差，为灵活、鲁棒的人机对齐提供可扩展的路径。

## 2. 方法论

- **核心思想**：利用 Transformer 的上下文学习能力，在推理时根据一小批偏好演示（提示）动态推断人类的线性奖励参数 θ*_i。
- **偏好模型**：采用 Bradley–Terry 模型，奖励函数为线性参数化 r_i(x,y) = φ(x,y)^⊤θ*_i，偏好由差值特征 ˜φ 决定。
- **初始架构的限制**：仅用二值偏好标签 z_i 构建提示矩阵时，Transformer 的预测依赖于 z_i 与 ˜φ 的乘积和，最终收敛到非线性统计量 μ(θ)，不能任意恢复 θ。理论上证明了存在偏好分布和参数，使得即使无限数据下也无法正确预测（渐近不可能）。
- **关键改进——反应时间**：根据漂移扩散模型，人做出比较决策的反应时间 t 满足 ˜φ^⊤θ*_i = (1/2) E[z_i | ˜φ] / E[t_i | ˜φ]。因此，z_i/t_i 可恢复关于偏好强度的线性信号。
- **新提示构造**：将每对比较的特征差 ˜φ 与聚合后的 z_i/t_i 构成提示矩阵，输入线性注意力 Transformer。
- **训练与预测**：用平方损失训练参数 U，目标预测 ô_i = (1/N)∑ (z_l/t_l) (˜φ_l)^⊤ U ˜φ_q。理论证明（定理 3、推论 1）当 N, K → ∞ 时 U* → Σ_˜φ^{-1}，使得对新未见人类 θ_new 的预测渐近无偏：ô_new → 2θ_new^⊤˜φ_q。

## 3. 实验设计

- **合成数据集**：特征差 ˜φ 随机采样，奖励参数 θ*_i 从两个高斯分布的混合中抽取（训练），OOD 测试用第三个不重叠的高斯分布。模拟漂移扩散过程（K 个独立注释者）生成反应时间。
- **真实数据集**：食物风险偏好数据集（Smith & Krajbich, 2018），包含 42 名参与者的二元选择及反应时间，特征为食物评分构造的多项式表示（R^5）。
- **对比设置**：
  - 仅使用二值标签（w/o resp） vs. 使用反应时间增强信号（w/ resp）。
  - 评估分布内（ID）和分布外（OOD）偏好下的测试准确率。
- **模型**：线性注意力 Transformer（与理论分析一致）和 GPT-2（124M 参数）。

## 4. 资源与算力

- 文中未提及 GPU 型号、数量或训练时长等具体资源消耗。仅指出使用了 GPT-2 124M 模型进行实验，但未给出算力细节。

## 5. 实验数量与充分性

- **实验组数**：两类场景（合成、真实），每种内部按是否使用反应时间、ID/OOD 划分，并变化推理长度 M（M=4, 8, 16）。合成数据上展示了线性注意力和 GPT-2 的结果，真实数据仅用 GPT-2 并画了误差棒。
- **消融与对比**：主要对比 w/ vs. w/o 响应时间，没有与其他多奖励或个性化奖励建模方法直接对比。实验规模较小，真实数据集只涉及 42 名被试的二选一任务，且未在不同任务领域测试。
- **客观性与公平性**：实验旨在验证理论发现，并非性能竞赛，对比设置简洁，能清楚反映信号引入的效果，但外部验证有限。

## 6. 主要结论与发现

- 仅靠二值偏好标签，Transformer 在上下文学习中无法适应未见的人类奖励参数（信息瓶颈导致非线性嵌入无法被线性解码）。
- 将反应时间作为辅助信号，通过 z_i/t_i 恢复偏好强度线性信息，理论上可从少量演示中零偏差地适应任意新偏好。
- 实验表明，反应时间增强显著提升 OOD 设置下的准确率，使 OOD 表现接近 ID 水平，并在 M 增大时效果更明显，验证了理论。

## 7. 优点

- **理论创新**：严格证明了二进制偏好标签情景下上下文学习的根本局限性，并给出了引入反应时间后可实现正确适应的理论保证。
- **跨学科融合**：将认知建模（漂移扩散）与机器学习形式化结合，挖掘反应时间的丰富信息。
- **实用导向**：提供了一条无需重新训练即可快速适应新用户偏好的可行路径，具有部署灵活性。
- **实验与理论一致**：在合成和真实数据上都复现了理论预期的现象，增强了可信度。

## 8. 不足与局限

- **模型架构简化**：理论分析仅针对线性注意力 Transformer，虽在 GPT-2 上得到验证，但缺乏对复杂架构的保证。
- **信号获取困难**：现实中收集可靠的反应时间存在噪声大、难以获取或成本高的问题，可能限制方法的直接应用。
- **实验覆盖不足**：仅在一个小型真实数据集上测试，未对比其他自适应或个性化方法，也未探索更多样的任务和领域。
- **单一辅助信号**：仅探讨了反应时间，未考虑其他潜在辅助信号（如眼动、脑电等），可扩展性有限。
- **特征假设**：依赖手工设计的特征表示 φ(x,y)，在通用文本生成任务中可能难以获得。

（完）
